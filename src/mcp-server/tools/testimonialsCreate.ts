/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { testimonialsCreate } from "../../funcs/testimonialsCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostApiTestimonialsSpaceIdRequest$inboundSchema,
};

export const tool$testimonialsCreate: ToolDefinition<typeof args> = {
  name: "testimonials-create",
  description: ``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await testimonialsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
