/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { testimonialsGet } from "../../funcs/testimonialsGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetApiTestimonialsRequest$inboundSchema,
};

export const tool$testimonialsGet: ToolDefinition<typeof args> = {
  name: "testimonials-get",
  description: ``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await testimonialsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
